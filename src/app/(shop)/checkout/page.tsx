// Checkout skeleton only. No card fields, no "Pay Now", no Stripe — payment is
// manual arrangement + crypto (NowPayments) only. See CLAUDE.md hard constraints.
export default function CheckoutPage() {
  return (
    <main className="mx-auto max-w-[820px] px-8 py-24">
      <h1 className="text-h1 font-bold">"Checkout — placeholder"</h1>
    </main>
  );
}
